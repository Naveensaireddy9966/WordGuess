services:
  - type: web
    name: wordguess-backend
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node dist/server.cjs
    rootDir: .
    envVars: []
    autoDeploy: true
